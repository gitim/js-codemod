'use strict';

const printOptions = {
  quote: 'single',
};

describe('single-quote', () => {

  it('transforms correctly', () => {
    test('single-quote', 'single-quote-test', {
      printOptions,
    });
  });

});
